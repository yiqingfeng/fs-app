{
	"name": "{{name}}",
	"version": "1.0.0",
	"description": "create a project for FS web",
	"main": "Gruntfile.js",
	"scripts": {
		"predeploy": "node ./tools/predeploy.js",
		"deploy": "node ./tools/deploy.js module=crm",
		"prestart": "cnpm install",
		"start": "gulp",
		"build-release": "gulp build-md5",
		"build-product": "gulp build",
		"build-development": "gulp build"
	},
	"dependencies": {
		"gulp": "^4.0.2"
	}
}
